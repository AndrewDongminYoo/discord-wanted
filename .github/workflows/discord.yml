# This is a basic workflow that is manually triggered

name: Manual workflow

# Ensure top-level permissions are not set to write-all (checkov/CKV2_GHA_1)
permissions: read-all

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Send greeting
      # cspell:ignore johnnyhuy
      # You may pin to the exact commit or the version.
      # uses: johnnyhuy/actions-discord-git-webhook@ba54912b26910da886c9095317d6da6182a87224
      uses: johnnyhuy/actions-discord-git-webhook@v1.0.0
      with:
        # Discord webhook URL
        webhook_url: https://discord.com/api/webhooks/1295030450829656064/Wcn-Jj3KaG2WALXBanNSFhEvrRtAaP2qklsmxMZT6FfqXLUSHNjamA9UvxSqovtMoo3C         
